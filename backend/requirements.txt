fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.6.1
# email-validator: requerido por pydantic.EmailStr (routers/auth.py) — sin esto la app NO arranca
email-validator==2.2.0
anthropic==0.40.0

# PostgreSQL
asyncpg==0.30.0

# Redis
redis[hiredis]==5.2.1

# Cifrado LOPDP — campos sensibles
cryptography==44.0.0

# HTTP client (Notion MCP)
aiohttp==3.11.10

# Auth: JWT + password hashing
python-multipart==0.0.19
python-jose[cryptography]==3.3.0
# bcrypt fijado en 4.0.1: passlib 1.7.4 lee bcrypt.__about__.__version__,
# atributo eliminado en bcrypt>=4.1 (provoca "error reading bcrypt version")
bcrypt==4.0.1
passlib[bcrypt]==1.7.4

# WhatsApp — Twilio
twilio==9.4.2

# Scheduler — recordatorios y tareas periódicas
apscheduler==3.10.4

# HTTP
httpx==0.27.2
